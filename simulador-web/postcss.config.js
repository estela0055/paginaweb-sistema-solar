// Configuración para PostCSS, una herramienta que transforma tu código CSS
export default {
  // Aquí se declaran los plugins que modificarán tu CSS antes de enviarlo al navegador
  plugins: {
    // Autoprefixer se encarga de agregar automáticamente los prefijos de navegador 
    // (como -webkit- o -moz-) a las reglas CSS para asegurar compatibilidad
    autoprefixer: {},
  },
}